import { moderateScale } from 'react-native-size-matters'

const PERSISTENT_HEADER_HEIGHT = moderateScale(48)
const USER_INFO_HEIGHT = moderateScale(170)
const BUTTONS_HEIGHT = moderateScale(56)

export { PERSISTENT_HEADER_HEIGHT, USER_INFO_HEIGHT, BUTTONS_HEIGHT }
